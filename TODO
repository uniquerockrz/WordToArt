* configurable characters except stars and spaces
* configuration file to read configurations
* configurable size of the output, currently its limited to 8 lines
* multiple lines of ASCII art, currently supports just one line
* symbols support and multiple character and smiley support like <3, :-) etc
* accept word from command line arguments
